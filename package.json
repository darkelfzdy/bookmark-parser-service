{
  "name": "bookmark-parser-service",
  "version": "1.0.0",
  "description": "A Cloudflare Worker to parse HTML bookmark files.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "typescript": "^5.0.4",
    "wrangler": "^4.22.0"
  }
}